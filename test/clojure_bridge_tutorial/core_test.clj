(ns clojure-bridge-tutorial.core-test
  (:require [clojure.test :refer :all]
            [clojure-bridge-tutorial.simple_values :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
